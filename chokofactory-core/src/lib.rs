//! Shared types used by `chokofactoryd` and `choco`: `Event`, DB models,
//! workflow definition types. Populated incrementally by later tickets
//! (see .agents/ChokoFactory/04-plan.md).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        assert_eq!(2 + 2, 4);
    }
}
